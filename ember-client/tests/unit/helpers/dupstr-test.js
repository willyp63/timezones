import { dupstr } from 'ember-client/helpers/dupstr';
import { module, test } from 'qunit';

module('Unit | Helper | dupstr');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = dupstr([42]);
  assert.ok(result);
});
